GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key-here
GROQ_API_KEY=your-groq-api-key-here


AI_PROVIDER=azure
AZURE_API_KEY='55RaJ'
AZURE_OPENAI_ENDPOINT=https://aganazureai.openai.azure.com
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_RESOURCE_NAME=aganazureai
AZURE_API_VERSION=2024-12-01-preview

AZURE_RESOURCE_NAME=aganazureai

# ─── Optional: override the model ID ─────────────────────────────────────────
# AI_MODEL=gpt-4o

# ─── Paths ────────────────────────────────────────────────────────────────────
# Set when the process starts from a different cwd (Docker, PM2, etc.)
# PROJECT_ROOT=/app

# ─── HTTP Server ──────────────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:5173
# MAX_BODY_BYTES=65536          # max /api/chat body in bytes (default: 64 KB)
# REQUEST_TIMEOUT_MS=120000     # LLM response timeout in ms (default: 120 s)

# ─── Rate Limiting ────────────────────────────────────────────────────────────
# RATE_LIMIT_WINDOW_MS=60000    # sliding window duration (default: 60 s)
# RATE_LIMIT_MAX=20             # max requests per session per window (default: 20)

# ─── Circuit Breaker ──────────────────────────────────────────────────────────
# CIRCUIT_BREAKER_THRESHOLD=5   # consecutive failures before breaker opens
# CIRCUIT_BREAKER_RESET_MS=60000 # how long breaker stays open (ms)

# ─── Logging ─────────────────────────────────────────────────────────────────
# LOG_LEVEL=info                # error | warn | info | debug