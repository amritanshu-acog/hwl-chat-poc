# Procedure PDFs

Drop **how-to / step-by-step procedure** PDFs here, then run:

```bash
bun run ingest --type=procedure ./docs/procedure/
```

The extraction engine will use `src/prompts/extraction.md` (procedure prompt).
